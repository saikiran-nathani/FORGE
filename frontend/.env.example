# Absolute URL of the FORGE backend API (no trailing slash).
#
# Local dev:  leave empty -> the Vite proxy forwards /api to http://localhost:8000
# Netlify:    set to your deployed backend, e.g. https://forge-api.onrender.com
VITE_API_URL=
